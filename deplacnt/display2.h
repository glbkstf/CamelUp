#ifndef DISPLAY2_H_INCLUDED
#define DISPLAY2_H_INCLUDED

//affiche le contenu (pour le dev uniquement)
void disp_pyramide(S_pyr pyramide[]);

//affiche le contenu (pour le dev uniquement)
void disp_position(int pos[]);

//affiche la piste de course
void disp_piste(S_case piste[]);

#endif // DISPLAY2_H_INCLUDED
